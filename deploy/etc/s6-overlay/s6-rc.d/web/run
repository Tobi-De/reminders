#!/command/with-contenv execlineb

cd /app
redirfd -r 0 create_user.py
foreground {
  /opt/pysetup/.venv/bin/python manage.py shell
}
/opt/pysetup/.venv/bin/gunicorn wsgi:application --bind 0.0.0.0:8000 --workers 2
